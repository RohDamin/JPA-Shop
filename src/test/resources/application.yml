spring: # 스프링부트는 아래 설정 없으면 그냥 메모리 DB 사용함
#  datasource:
#    url: jdbc:h2:mem:test #테스크 케이스를 격리된 환경에서 실행하기 위해 메모리 DB 사용
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

